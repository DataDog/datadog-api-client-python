# Unless explicitly stated otherwise all files in this repository are licensed under the Apache-2.0 License.
# This product includes software developed at Datadog (https://www.datadoghq.com/).
# Copyright 2019-Present Datadog, Inc.
from __future__ import annotations

from typing import Union

from datadog_api_client.model_utils import (
    ModelNormal,
    cached_property,
    unset,
    UnsetType,
)


class RUMOperationJourneyNode(ModelNormal):
    @cached_property
    def openapi_types(_):
        return {
            "id": (str,),
            "query": (str,),
        }

    attribute_map = {
        "id": "id",
        "query": "query",
    }
    read_only_vars = {
        "id",
    }

    def __init__(self_, query: str, id: Union[str, UnsetType] = unset, **kwargs):
        """
        A single node within a RUM operation journey step, matching RUM events with a query.

        :param id: The unique identifier of the node. Generated by the server if omitted.
        :type id: str, optional

        :param query: The RUM search query used to match events for this node.
        :type query: str
        """
        if id is not unset:
            kwargs["id"] = id
        super().__init__(kwargs)

        self_.query = query

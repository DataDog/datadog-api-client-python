    @classmethod
    def _from_openapi_data(cls{{#requiredVars}}{{^defaultValue}}, {{name}}{{/defaultValue}}{{/requiredVars}}, *args, **kwargs):
        """Helper creating a new instance from a response."""
{{#requiredVars}}
{{#defaultValue}}
        {{name}} = kwargs.get('{{name}}', {{{defaultValue}}})
{{/defaultValue}}
{{/requiredVars}}

        self = super({{classname}}, cls)._from_openapi_data(kwargs)

{{> model_templates/invalid_pos_args }}
